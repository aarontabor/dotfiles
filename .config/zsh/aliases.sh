# Use nvim
alias vim=nvim
alias vi=nvim

# Single Key Aliases
alias d=docker
alias f=find
alias g=git
alias j=jobs
alias l=ls
alias v=vim

# Custom Definintions
function take {
  mkdir -p $1 && cd $1
}
